import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'CheckList porte tagliafuoco',
  webDir: 'www'
};

export default config;
